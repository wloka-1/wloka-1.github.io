# wesley-portfolio

Wesley's personal portfolio site. Single self-contained `index.html`, no build step, designed for GitHub Pages hosting.

## Status (2026-06-02)

V4 content draft complete and ready to deploy to GitHub Pages. Awaiting:

- Repo creation + first push (Wesley will do this when he's back)
- Hosted resume PDF link (still points to `#`)
- Headshot
- Project imagery (BNY is NDA-limited; Qualcomm/T-Mobile/Overlay can use Figma exports)

Built specifically to support a Staff/Senior Product Designer (AI-forward) job search. Replaces the old Figma proto walkthrough as the canonical portfolio URL on Wesley's resume.

## What lives here

- `index.html` — the entire site. Newsreader serif + Manrope + Space Grotesk. Single page with hash-routed subpages for each case study (BNY, Qualcomm, T-Mobile, Overlay).
- `README.md` — preview + deploy instructions.

## Hard content rules

- **No em-dashes.** Anywhere. The separator everywhere is `·` (interpunct). Wesley reads em-dashes as an AI tell.
- **NDA scrub (2026-07-13, applies to site AND resume):** never say Eliza is "built on Claude" or name BNY's stack; never use "digital employee" / "DE" (say "agent marketplace", "AI agents"); no internal URLs (AI.bny), internal org taxonomy (Practices/Platforms/Hubs), named top-level nav areas, exact accuracy thresholds, or named agent task types. Wesley's *personal* workflow claims ("Claude → code, no Figma", footer, sketch-to-revit) are fine and stay.
- **BNY frameworks that ARE Wesley's (ok to claim):** hierarchical IA that was adopted; placement framework (Eliza-native / integrated / external · verify middle term with Wesley); task/notification model (self-created / assigned / AI-proposed); 1:1 user testing of nav + notifications. Current role = present tense for ongoing ownership, past tense for shipped events.
- **Voice:** direct, blunt, lowercase-okay, not corporate, no LinkedIn-speak. Match Wesley's own writing.
- **Claude → code, no Figma.** State it confidently in the hero and How I Work. This is the load-bearing positioning differentiator.

## Attribution corrections (must not violate)

Two pieces of BNY work are **not** Wesley's:

1. The specific four-layer design-system architecture and 73 component contracts were built by Scott during Wesley's paternity leave (mid-April to end of May 2026). Wesley shaped the *strategy* before paternity leave (anti-Tailwind decision, LLM-friendly component direction, semantic vs primitive thinking). That's what's on the portfolio. Do not claim the architecture or contracts.
2. The five-question alignment-brief intake framework was created by Wesley's boss during paternity leave. It is not on this site and should never be.

These are also captured in Cortex memories under `sketch-to-revit` project.

## Related projects

- `~/Documents/Documents - fuo/Resume/Wesley Kay Resume - AI Forward.md` — the resume that links to this portfolio.
- `~/Projects/sketch-to-revit/` — one of the two side projects highlighted on the homepage.
- Iona Tracker (no project folder yet; lives on Wesley's Mac mini).

## How to help

- Treat this as a working asset for active job applications. Changes need to be safe to ship.
- When updating content, verify against the resume markdown and Obsidian meeting notes. The portfolio and resume must stay aligned.
- Don't add em-dashes. Don't add LinkedIn-speak. Don't soften Wesley's voice.
- If something is genuinely Wesley's, name it directly. If it's not, leave it out.
